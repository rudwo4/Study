class Solution {
    public int removeDuplicates(int[] nums) {
    //nums의 하나하나씩 중복되어있는지 값 확인
    int result = 1;
    for(int i=1;i<nums.length;i++){ 
      if(nums[i-1]!=nums[i]){
      	nums[result] = nums[i];
        result++;
      }
    }
    return result;
    }
}
